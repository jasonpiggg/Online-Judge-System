# 项目协作约定

## 回复与检查

- 冷静、专业、直接，省略客套和重复总结。
- 技术问题优先给出可运行代码和关键 Inline 注释，保留标准英文术语。
- 读取日志、JSON 或搜索结果时先过滤、聚合并限制输出。
- 保留与当前任务无关的用户改动；不使用破坏性的 Git 重置或强制推送。

## GitHub 同步流程

- GitHub 协作内容统一使用英文，包括分支名称、commit message、PR 标题与描述、review comment、
  CI/验收摘要及后续补充说明；使用 Conventional Commits，不新增中文 GitHub 元数据。
- 已合并历史不得仅为翻译而改写 Git commit；可编辑的 PR 元数据应保持英文。产品界面文案、
  本地化内容和面向中文读者的项目文档不受此约束，按其目标语言编写。
- 用户已要求后续开发同步 GitHub：每轮实现和验证完成后，提交本轮相关代码、测试与文档，
  并推送到远端功能分支。只读解释、诊断或计划任务不因此获得修改代码的授权。
- 开始开发前检查工作区、当前分支与远端状态；新建功能分支默认使用 `codex/` 前缀。
- 提交前审查差异并执行相关测试、Ruff、mypy 和 `git diff --check`。
- 不提交 `.env`、真实 API key、加密主密钥、用户数据库、会话、日志或临时验证产物；
  仅提交不含秘密的配置模板。不要在输出或 PR 描述中展示秘密。
- 按逻辑拆分提交，使用明确的 Conventional Commit 消息。推送后创建或更新对应 PR，
  说明范围、验证证据、风险与尚未完成的验收；跟进 CI 并修复本轮引入的问题。
- 用户已授权将以下交付流程设为后续默认：提交并推送 → 创建/更新 PR → 自动 code review
  → 修复阻塞问题 → 复审和 CI 全部通过 → 合并到 `main` → 同步本地 `main`。
- 自动 code review 必须实际检查 PR 差异、正确性、安全边界、回归风险与测试证据；
  不能将 CI 全绿等同于审查通过。审查结论及修复记录写入 PR，准确标明是自动审查，
  不冒充独立人工批准；PR 作者无法批准自己时记录 COMMENT，不绕过审批要求。
- 合并前确认最新 head 提交与已审查、通过 CI 的提交一致，检查合并冲突、阻塞评论与
  分支保护要求。使用普通 merge commit 保留逻辑提交；不使用管理员绕过、强制推送。
- 合并后检查 `main` 的 CI，用 fast-forward 同步干净的本地 `main`；存在用户未提交修改
  时保留修改并报告，不自动 stash、丢弃或覆盖。下一轮开发新建功能分支。
- 该授权仅覆盖当前任务相关的 PR，不包含其他 PR、依赖升级、发布版本或部署到外部环境。
- 如遇冲突、权限、网络或测试阻塞，明确说明已完成步骤与尚未同步内容，不宣称成功。
- 完成时提供分支、提交、PR 链接和 CI 状态。
