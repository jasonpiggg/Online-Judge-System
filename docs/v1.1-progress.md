# v1.1 修复与前端重构执行记录

## 已确认的目标

浅灰白主界面（#F5F7FB）、深蓝侧栏（#14243A）、蓝色主操作（#3563E9）。
正文 16px、侧栏 17px/600、导航行高 48px、控件至少 44px。
保留 Streamlit/FastAPI、课程 API、现有数据与指定管理员；不引入 React。

## 原计划与检查点

- [x] PR #9 `fix/judge-correctness`：空白保真、三级资源限制、有界输出、进程清理；Linux CI 通过并合并。
- [x] PR #10 `feat/frontend-foundation`：主题、侧栏、页面导航、响应式题库/同屏做题、账户页面；CI 通过并合并。
- [x] PR #11 `feat/frontend-workflows`：可视化样例/测试点编辑、草稿、预览、删除确认、分页、管理员重测/筛选、管理中心；CI 通过并合并。
- [x] PR #12 `fix/ai-authoring-quality`：真实双阶段生成/审查、至少 5 个独立输入、参考解和两种错误解验证、累计计费、真实取消、重启失败恢复、编辑器导入新增/更新；CI 通过并合并。
- [x] PR #13 `chore/security-hardening`：备份与版本迁移、角色审计、连接目标防 SSRF、依赖锁定和漏洞扫描、Dependabot；CI 通过并合并。
- [x] `test/v1.1-release`：Linux 98 个测试全部通过，后端行覆盖率 97.67%、分支 93.07%；完成权限矩阵、完整 verdict、真实 HTTP AI mock、AppTest、三尺寸浏览器验收、截图及 Markdown/PDF 报告。最终 PR/CI/Release 状态以 GitHub 记录为准。

每个阶段从最新 main 建分支，Conventional Commits → PR → CI 成功 → merge commit → 删除分支。
最终保留 v1.0.0 并发布 v1.1.0，记录最终 SHA；不将未执行的真实供应商验收写成通过。

## 界面验收必须覆盖

1440×900、1024×768、390×844；普通用户与管理员分别测试。
题库搜索/分页 → 同屏题面和 Ace 编辑器 → pending/AC/非 AC → 详情/日志。
题目分区编辑与样例/测试点增删、JSON 高级导入、继承限制、预览、保存、管理员删除确认。
全用户提交筛选和重测、用户角色、语言注册、审计筛选、日志公开真实值、reset 二次确认。
AI 阶段进度/取消/费用、可读结果、人工审阅、载入标准编辑器、新增或确认更新。
无图标原始名称、无页面级横向溢出、无窄屏侧栏遮挡；终态停止轮询；会话内草稿不丢失。

## 2026-09-03 执行中断排查

两次执行在浏览器刷新附近中断，最后一批文件均已写入，未出现项目测试失败导致终止的证据。
Codex 本地日志记录工具返回结果缺失、执行层重连，以及桌面 ResizeObserver 错误；尚不能证明唯一根因。
采用短调用和热更新继续；不修改 Codex 全局设置、不清除用户数据、不伪造验收。

## 2026-09-04 恢复与最终验收

- 恢复时 8010、8090、8510 均无监听进程，没有遗留后台任务；工作树完整停在 `test/v1.1-release`。
- Windows 回归为 88 passed、10 skipped；跳过项均为 Linux runner 专属。WSL2 / Python 3.12.3 为 98 passed，无跳过。
- 真实浏览器检查 1440×900、1024×768、390×844；三档 `scrollWidth` 均等于 viewport 宽度，未发现页面级横向溢出。桌面侧栏 256px、正文 16px，手机标题 26px且侧栏默认收起。
- 普通用户实际完成注册、登录、题目进入、Python 提交、结果/记录与草稿保持；管理员实际完成全用户记录、重测、角色修改、日志公开和 AI 新增/更新入库。
- AI 使用本地 HTTP/SSE mock 实际完成双阶段调用、usage 汇总与本地验证；另在 UI 中中断运行中任务，终态显示“已取消”且保留已观察费用。
- 删除与 reset 均实际打开二次确认并取消，确认无副作用；真正删除/reset 由 API 自动测试覆盖，避免在最终截图数据集上执行不可逆 UI 操作。
- 浏览器发现并修复直接启动缺少仓库 `PYTHONPATH`、Windows Git 将 WSL shell 脚本转成 CRLF、不同账号复用同一 Streamlit 会话时瞬态草稿可见三个问题，并补回归或启动验收。
- 修复后在 WSL 实际运行 `scripts/run.sh`，API `/docs` 与 Streamlit `/_stcore/health` 均返回 200，Ctrl+C 后两个端口均释放。
