# AI 局部修改失败排查与修复（2026-09-04）

## 实际失败记录

- 任务：`ai-XEAlIqGZ6lxtwm_W`，题目 `brackets`（《括号的秩序》）。
- 操作：`revise / samples`。
- 需求：“给多点样例吧，把题目中描述的情况展示得更清晰一点”。
- 第一阶段返回 `finish_reason=length`，输入 833 / 输出 16384 Token，记录费用 CNY 0.465416。
- 不是鉴权失败或断网：服务商返回了完整用量；模型耗尽输出预算。
- 根因：局部补样例误用完整命题提示词，要求同时生成整题、规模测试、参考解、oracle、
  随机生成器和错误算法，并重复发送已有题目与草稿。补样例不应承担整题重建成本。

## 修复

1. 样例、题面局部修改采用单独提示词和严格 Schema，只合并白名单字段。
   既有测试点、限制、题号及其他字段保持不变，模型不能注入额外字段修改它们。
2. 局部上下文不携带隐藏测试点，不重复发送同一题目。初稿 Flash，复审高质量模型；
   困难信号仍可触发高质量初稿。局部输出预算默认 8192 Token。
3. 展示样例采用小输入，最多 20 条，禁止重复输入，每条输入/输出不超过 2000 字符。
4. 复审失败保留已完成初稿；截断输出保留下载，不能当成已验证结果发布。
5. 任务页区分“局部建议”和“整题质量门禁通过”，提供明确确认费用的手动重新发起入口。
   采纳时检查来源草稿版本，保留原有算法资产并使旧验证结论失效。
6. 完整命题仍运行原有本地质量验证，没有通过降低质量门禁掩盖错误。

## 同需求真实验证

使用相同题目、相同需求，在临时数据库中单独测试；没有重新执行或覆盖原任务，
没有发布或修改正式题库，也没有自动重试付费请求。

| 阶段 | 模型档位 | 输入 Token | 输出 Token | CNY 估算费用 |
| --- | --- | ---: | ---: | ---: |
| 样例初稿 | Flash | 404 | 286 | 0.000562 |
| 样例复审 | GLM-5.3 | 695 | 766 | 0.027008 |
| 合计 | 分阶段计价 | 1099 | 1052 | 0.027570 |

最终状态 completed，获得 10 个样例。另用独立栈算法核对每个输入对应的 YES/NO，全部一致。
这证明本次局部修复流程可用，不代表任何模型输出在所有任务中都不会出错。
费用按配置及服务商 Token 用量计算，实际扣费以服务商账单为准。

新增测试覆盖局部字段保真、超大/重复样例拒绝、缺少原题不付费调用、复审失败保留初稿、
截断输出保留、前端建议/下载展示及手动重试确认。真实服务重启前检查无运行中 AI 或评测任务。
最终回归 146 passed、10 skipped（Windows 平台相关跳过）；Ruff、mypy、diff 检查通过。
局部修改相关 8 项测试单独复验通过。采纳建议也会阻止覆盖编辑器中不同的未保存内容。

以后可在原失败任务下展开“按原需求重新发起（会产生新费用）”，明确确认后发起新任务。
原记录及费用不会被删除，也不会被伪装成成功。
